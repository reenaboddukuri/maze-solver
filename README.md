# Maze Solver: 
- Program that helps a robot efficiently navigate through a custom maze and escape
- Implemented and enhanced the robot’s navigation using recursion, backtracking, the right-hand rule, and cloning robot states to recursively explore potential pathways

## Folder Structure

The workspace contains two folders by default, where:

- `src`: the folder to maintain sources
- `lib`: the folder to maintain dependencies

Meanwhile, the compiled output files will be generated in the `bin` folder by default.
